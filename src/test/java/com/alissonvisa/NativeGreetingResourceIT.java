package com.alissonvisa;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGreetingResourceIT extends PersonResourceTest {

    // Execute the same tests but in native mode.
}